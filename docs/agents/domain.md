# Domain docs

本仓库采用 single-context 文档布局。

## 开始工程工作前

先阅读根目录的 `CONTEXT.md`，再阅读与当前工作相关的 `docs/adr/`。若文件不存在则继续，不需要预先创建占位文档。

## 使用术语

issue、实现、测试和讨论应采用 `CONTEXT.md` 已定义的术语，避免为同一概念引入同义词。若确有缺失，应标注为领域文档缺口，而不是自行固定新术语。

若拟议方案与 ADR 冲突，必须明确指出冲突及原因，不得静默覆盖已有决策。
