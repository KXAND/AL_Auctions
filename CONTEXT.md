# 竞拍游戏

本上下文定义多人竞拍游戏中包裹、连续竞拍与结算相关的领域语言。

## 代码术语约束

代码中的类型、字段、参数、局部变量、资源名和 Unity 菜单名均遵守本节。新代码不得继续引入同义词；修改现有模块时，应在该模块范围内统一为标准术语，不保留兼容别名。

### 标准术语

| 标准术语 | 含义与用法 | 禁用同义词 |
| --- | --- | --- |
| `Item` | 单件藏品的统一英文词根；组合名称只替换该词根并保留职责后缀 | `Collectible` |
| `ItemData` | 公开藏品目录中的一项静态配置 | `ItemDefinition`、`definition` |
| `ItemCatalog` | `ItemData` 的目录 | `CollectibleCatalog` |
| `ItemId` | 标识一种 `ItemData` 的稳定 ID | `CollectibleId` |
| `ItemInstanceId` | 标识某个包裹内一件藏品实例的 ID | 无上下文的 `InstanceId` |
| `Match` | 一局竞拍；玩法代码统一使用该词；`Game` 只用于应用级概念，例如 `GameManager` | 用 `Session` 或宽泛的 `Game` 表示具体对局 |
| `Session` | 仅表示 Fusion 的网络会话，不表示一局竞拍 | 用于玩法对局或玩法阶段 |
| `Round` | 对局状态中的当前竞拍轮序号 | — |
| `RoundId` | `ActionRequest` 所指向的竞拍轮 | — |
| `Participant` | 一局中固定的参赛者；参赛者顺序由 `MatchParticipant` 列表顺序确定 | `Seat`、`PlayerSlot` |
| `Controller` | 为一个参赛者产生并提交动作的真人或 AI 控制者 | `Participant` |
| `PlayerIdentity` | 当前连接或控制者用于请求路由的运行期身份 | `AssetOwnerId` |
| `AssetOwnerId` | 跨对局持有资产的主体 ID；可能与 `PlayerIdentity` 不同 | `PlayerIdentity` |

`TargetPlayer` 是 `AuthorityResult`、`AuthorityState` 等定向消息中目标 `PlayerIdentity` 的角色名。赢家字段保存 `AssetOwnerId`，统一命名为 `WinnerAssetOwnerId`。

### 消息术语

- `AuthorityState`：带 `StateVersion` 的定向状态消息。
- `VisibleRecord`：Authority 为一个目标玩家生成的不可变可见记录。
- `SettlementRecord`：用于幂等资产持久化的权威结算记录。
- `SettlementView`：提供给玩家展示的结算投影。
- `Request`、`Result`：一次请求及其对应处理结果。

### 当前代码中的待统一名称

以下名称仍存在于代码中，属于已确认的命名债务；后续重构时应直接替换，不新增兼容层：

- `CollectibleCatalog` 应改为 `ItemCatalog`；`VisibleCollectible` 应改为 `VisibleItem`；`CollectibleId` 应改为 `ItemId`；`collectibles`、`collectibleCatalog` 分别应改为 `items`、`itemCatalog`。
- `GetAllDefinitions()`、`definition`、`definitions` 以及 `Game/Item Definition` 应统一到 `ItemData` 术语。
- `SessionPhase` 表示玩法对局阶段，应改用 `MatchPhase`；`Session` 只保留给 Fusion 网络会话。
- `WinnerOwner` 实际保存资产所有者 ID，应改为 `WinnerAssetOwnerId`。
- `SeatId` 不再属于现行模型，应从 `MatchParticipant`、Authority 内部状态及相关校验中移除；参赛者稳定顺序使用列表顺序。
- `RoundSeatReveal` 应改为 `RoundParticipantReveal`，相关局部变量使用 `participantReveal`。
- `OnCollectionClicked()` 实际打开图鉴目录，应改为 `OnCatalogClicked()`。

## 开发验证

**本地试玩**：
开发阶段中由玩家显式开始、以一个本地玩家身份和 AI 补齐其余 `P - 1` 名参赛者进行完整对局的单人验证方式；同一次试玩跨对局保留本地玩家资产，重置试玩时重新分配初始资产。
_避免_：同机多人、联机验收

**联网验收**：
在本地试玩已验证玩法后，以多个客户端连接联网对局来验证同步和连接行为的方式。
_避免_：日常玩法验证、本地试玩

## 竞拍

**对局**：
围绕一个固定包裹进行的完整游戏过程，直到产生赢家或以未成交结束为止。
_避免_：多包裹局、固定五轮游戏

**对局人数**：
一局对局包含的参赛者数量，以 `P` 表示，是可配置规则；当前默认值为 `4`。
_避免_：硬编码四人局

**包裹**：
一局对局中唯一且不变的拍卖标的，由多个藏品构成，并具有未公开的包裹总价值。
_避免_：回合拍品、新包裹

**包裹总价值**：
包裹内所有藏品具体价值之和，以 `Y` 表示。
_避免_：估值、出价

**竞拍轮**：
围绕同一包裹依次进行分析阶段、出价阶段和回合揭示阶段的一次竞拍；仅在出现获胜出价时结束整局对局。
_避免_：独立拍卖、独立包裹

**分析阶段**：
竞拍轮中公开公共线索、玩家选择私有线索并立即获得自身结果的封闭信息阶段。
_避免_：出价阶段

**出价阶段**：
竞拍轮中玩家依据已知信息提交出价、等待统一结算的封闭信息阶段。
_避免_：分析阶段

**阶段倒计时**：
分析阶段、出价阶段与回合揭示阶段各自具有的有限完成时限；分析或出价时限届满后不再等待未响应玩家。
_避免_：无限等待

**分析超时弃权**：
玩家未在分析阶段倒计时内选择私有线索时，放弃该轮私有线索的规则。
_避免_：自动选择线索

**出价超时出价**：
玩家未在出价阶段倒计时内提交出价时，视为提交 `0` 出价的规则。
_避免_：沿用上一轮出价、自动估价

**公共线索**：
每个竞拍轮开始时向所有玩家公开的包裹信息。
_避免_：共享私有线索

**私有线索**：
每名玩家在竞拍轮中为自己获取、且仅该玩家可见的包裹信息。
_避免_：公共线索、全员可见线索

**私有线索配额**：
未超时的每名玩家在每个竞拍轮恰好获取一条私有线索的规则。
_避免_：任意数量线索

**共享选择**：
同一种私有线索可在同一竞拍轮被多名玩家选择，且不因先后顺序而被占用。
_避免_：抢线索、独占线索

**线索池**：
可被竞拍轮随机抽取候选私有线索类型的集合，允许同一类型在同一对局的不同轮次重复出现。
_避免_：固定线索菜单、单局去重线索池

**候选线索集**：
在一个竞拍轮中从线索池无放回随机抽取、供玩家选择的 `n` 条互不重复的私有线索类型；`n` 是全局固定配置，跨轮仍允许重复出现。
_避免_：永久可选线索

**共享候选线索集**：
同一竞拍轮内由全体玩家共同面对的一组候选线索集。
_避免_：个人候选线索集

**公开选择、结果私密**：
私有线索的类型在竞拍轮结算时向所有玩家公开，但该线索实际揭示的信息只向选择者公开。
_避免_：全隐私线索、全公开线索

**回合揭示**：
出价阶段结束后统一公布全体玩家的出价、所选私有线索类型与竞拍结果的只读阶段；持续固定时间，期间不接受参赛者动作，结束后进入下一轮分析或整局结算。
_避免_：实时出价公布、实时线索选择公布

**获胜倍数**：
当前轮最高有效出价相对第二高有效出价所必须达到的最低比例，由对局规则配置；当且仅当 `最高有效出价 ≥ 获胜倍数 × 第二高有效出价` 时成交。当前默认序列为 2、1.6、1.3、1.1，第五轮及之后固定为 1。
_避免_：加价幅度

**同价决胜**：
最高出价并列时，按出价先后顺序确定赢家的规则。
_避免_：随机决胜、无人获胜

**出价名次**：
按有效出价金额降序、同价按有效出价顺序升序形成的名次；第二高出价取第二名的出价，因而 `100、100、10` 的第二高出价为 `100`。
_避免_：跳过同价的第二高出价

**有效出价顺序**：
竞拍权威认可出价的唯一先后顺序，用于同价决胜。
_避免_：客户端点击时间、本地时间

**竞拍权威**：
对局中唯一认可参赛者动作、生成竞拍结果并更新对局状态的裁定方。
_避免_：客户端本地裁定、玩家房主裁定

## 参赛者

**参赛者**：
对局中持有已知信息和出价记录的固定参与者，其控制者可以是真人或 AI；参赛者顺序由开局时的 `MatchParticipant` 列表顺序确定，不单独建模 `Seat` 或 `SeatId`。
_避免_：客户端连接、单纯 AI 实例

**玩家资产**：
归属于 `AssetOwnerId` 而非参赛者或 AI 控制者的资产；长期由账号系统跨游玩持有。
_避免_：参赛者资产、AI 资产

**AI 接管**：
真人控制者在对局中断线后，由 AI 完成本局剩余操作以维持其他玩家的游戏性；断线玩家不从该局 AI 操作中获得收益。
_避免_：暂停对局、断线玩家收益

**AI 结算弃置**：
断线后接管参赛者控制权的 AI 自身利润或亏损直接弃置；若其高估包裹，仍按亏损分配规则影响所有非赢家参赛者。
_避免_：断线玩家继承 AI 收益、AI 不参与结算

**断线后新身份连接**：
真人控制者断线后，其对旧对局的参与即告结束；当前阶段的重连视为新的玩家身份，不恢复旧对局控制权、资产或其他关联。
_避免_：对局中控制权返还、旧身份重连

**参赛者动作**：
参赛者在竞拍轮中可提交的领域动作，包括选择私有线索与提交出价。
_避免_：控制者专属动作

**参赛者控制者**：
为参赛者决定并提交参赛者动作的真人或 AI。
_避免_：规则系统、特定 AI 实现

**控制者可替换性**：
同一参赛者可更换真人或任意 AI 实现，而不改变竞拍规则、同步状态或其他玩家可见的对局语义。
_避免_：将 AI 类型作为对局规则

**开局 AI 补员**：
对局开始时，真人参赛者不足的部分自动创建由 AI 控制的参赛者。
_避免_：等待满员、缺参赛者开局

**开局后参赛者锁定**：
对局开始后，参赛者及其真人或 AI 控制关系不再接受新连接者替换；新连接者仅可等待下一局。
_避免_：中途加入、中途替换 AI

**可用资产**：
玩家当前可用于提交出价的资产额度，不允许通过出价透支。
_避免_：信用额度、可用负债

**运行内资产**：
当前账号系统尚未实现时，客户端每次连接服务端或每次本地试玩获得的固定初始玩家资产；同一次连接或试玩内可跨对局累积，重新连接或重置试玩时重新分配。
_避免_：参赛者资产、跨重连资产保留

**零资产参赛者**：
运行内资产为 `0` 但仍留在对局与后续对局中的参赛者；可参与分析并放弃竞拍，也可获得亏损分配。
_避免_：淘汰参赛者、强制补款

**有效出价**：
大于 `0`、不高于该玩家可用资产且为整数最小货币单位倍数、可参与竞拍胜负判定的出价。
_避免_：透支出价、零元弃权、负数出价

**放弃竞拍**：
玩家在出价阶段提交 `0`，不参与当轮胜负判定的行为。
_避免_：零元有效出价

**单人有效竞价**：
仅有一个玩家提交有效出价时，第二高有效出价按 `0` 计算，该玩家在任意竞拍轮直接成交。
_避免_：等待最低获胜倍数

**未成交轮**：
存在多个有效出价但最高出价未满足当轮获胜倍数的竞拍轮；不发生任何参赛者资产变化并进入下一轮。
_避免_：试探出价成本、冻结资产

**全员弃拍**：
所有玩家在同一竞拍轮放弃竞拍、因而不产生赢家的结果；对局进入下一竞拍轮。
_避免_：零元成交、第五轮流拍结束

**连续弃拍上限**：
第五轮及之后允许连续全员弃拍的可配置最大轮数；达到上限时，对局以未成交结束。
_避免_：无限续轮、固定第五轮结束

**未成交结束**：
因达到连续弃拍上限而结束的对局结果，不发生任何参赛者资产变化。
_避免_：零元成交、亏损分配

## 包裹构成

**固定宽度网格**：
用于摆放包裹藏品的整数格棋盘，其宽度在当前规则中固定且可配置。
_避免_：随机棋盘宽度

**自动增长高度**：
固定宽度网格的高度随包裹藏品布局扩展，直至能够容纳全部藏品的规则。
_避免_：固定高度棋盘

**藏品**：
包裹的组成物，以占据整数格的矩形呈现，并具有珍稀度和具体价值。
_避免_：非网格资源

**非重叠摆放**：
不同藏品占据的整数格互不重叠，每个整数格至多属于一件藏品。
_避免_：叠放藏品

**列方向无空洞**：
任一被藏品占据的格子，其同一列上方的所有格子也被藏品占据的布局规则。
_避免_：悬空藏品、中间空洞

**随机摆放**：
藏品在网格中的位置由随机布局生成，位置不包含关于珍稀度的固定规律。
_避免_：按珍稀度排序、固定位置价值

**珍稀度**：
藏品的 `N`、`R`、`SR` 或 `SSR` 等级，以颜色呈现并用于向玩家暗示其具体价值；金色等同于 `SSR`。
_避免_：独立颜色分类、具体价值、出价

**具体价值**：
预定义藏品实际贡献给包裹总价值的固定数值，不在对局内随机生成。
_避免_：对局内随机价值、珍稀度、玩家估值

**预定义藏品**：
在设计阶段确定其矩形尺寸、珍稀度和具体价值的藏品目录项。
_避免_：对局内随机生成的藏品

**公开藏品目录**：
所有玩家在对局外共同可查阅的预定义藏品目录，公开名称、外观、尺寸、珍稀度和具体价值。
_避免_：隐藏局外价值表

**动态包裹**：
对局开始时按规则从预定义藏品目录抽取内容并随机摆放所生成的包裹。
_避免_：预制包裹组合

**默认网格视图**：
玩家在没有线索时只能看到包裹网格，不可见任何藏品的位置、形状、珍稀度或具体价值。
_避免_：默认展示藏品信息

**珍稀度形状线索**：
揭示某一珍稀度的每件藏品的矩形边界、形状和珍稀度，但不揭示其具体身份或具体价值的线索。
_避免_：仅占格蒙版、完整藏品揭示

**随机完整揭示线索**：
随机选取一件藏品实例并向线索持有者揭示其位置、形状、珍稀度、身份和具体价值的线索；不连带揭示同目录项的其他实例。
_避免_：珍稀度形状线索

**对局内知识**：
参赛者在本局中经公共线索或私有线索获得、并持续保留至对局结束的信息。
_避免_：每轮重置的信息、局外永久知识

**参赛者视角**：
一个参赛者可用于决策的全部信息，即公共信息与该参赛者自身的对局内知识，不包含包裹真值或其他参赛者的私有信息。
_避免_：权威真值视角、全知 AI

**公共线索池**：
用于在每个竞拍轮随机抽取公共线索的线索集合，允许单局内重复抽取，也允许与私有线索提供重叠信息。
_避免_：单局去重公共线索、强制公私隔离

**包裹生成约束**：
动态包裹必须同时满足的可配置条件，包括包裹总价值区间和按珍稀度划分的藏品总价值区间。
_避免_：无约束随机抽取

**可重复抽取**：
同一预定义藏品目录项可在同一动态包裹中被抽取多次的规则。
_避免_：同包裹类型唯一

**自动资产结算**：
竞拍成功后系统直接以 `Y - Xw` 改变赢家的资产余额，而无需玩家手动处理包裹或价值。
_避免_：手动兑换、逐藏品计价结算

**结算揭示**：
结算时展示包裹藏品与价值构成的表现层信息，不影响资产结算。
_避免_：逐藏品经济结算

**赢家损失**：
赢家出价高于包裹总价值时产生的 `Xw - Y` 资产损失。
_避免_：未成交损失

**亏损分配比例**：
从赢家损失中分配给其余参赛者的可配置比例，以 `r` 表示。
_避免_：固定硬编码比例

**亏损分配**：
其余每名参赛者获得向下取整的 `r × (Xw - Y) ÷ (P - 1)` 整数资产变化；当前 `P = 4` 时为三等分，未分配部分和除法余数离开本局经济系统。
_避免_：赢家均分、全额返还

**最小货币单位**：
资产、藏品具体价值和出价共同使用的不可分割整数计价单位。
_避免_：浮点资产、未定义的小数舍入
